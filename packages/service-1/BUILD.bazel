load("@npm//:defs.bzl", "npm_link_all_packages")
load("//tools/ts:defs.bzl", "service")

package(default_visibility = ["//visibility:public"])

npm_link_all_packages(name = "node_modules")

service(
    name = "service-1",
    deps = [
        "//packages/_libraries/lib-1:pkg",
    ],
)
